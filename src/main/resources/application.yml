spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
  datasource:
    url: jdbc:postgresql://localhost:5432/springshop
    username: postgres
    password: sa
    driver-class-name: org.postgresql.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: gmail@user.com
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true